def format_call(formatter, name, properties, children_ids, indent_level, indent):
    args = " ".join([f"${arg}" if not arg.startswith("'") and not arg.startswith('"') else arg for arg in properties.get("args", [])])
    return f"{indent}{name} {args}"